# Archived RES evidence

Human-readable reports and preregistrations are copied from `NeonInsight/evals` at commit `1187ead1fa13a4af76433ca32b8aad30cd3d52ef`. Three aggregate JSON reports are included alongside them. See [source-manifest.json](source-manifest.json) for exact upstream paths, blob identities, URLs, and local hashes.

These are historical records. The frozen gate remains failed; the favorable initial diagnostic did not replicate in the later five-factor comparison. [Current findings](../docs/findings.md) supplies the dated public interpretation.

The archive preserves source text, including historical relative links. Follow each file's immutable upstream URL in the manifest when a relative link refers to a runner or raw artifact outside this documentation archive. The full experimental package remains at [the fixed research snapshot](https://github.com/NeonInsight/evals/tree/1187ead1fa13a4af76433ca32b8aad30cd3d52ef).

Upstream experiment material retains its MIT license in `UPSTREAM-LICENSE.md`. Local copies are byte-checked against their recorded Git blob identities. SHA-256 hashes show byte identity, not independent timestamps or scientific validation.

